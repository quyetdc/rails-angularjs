<nav class="navbar navbar-inverse" role="navigation">
  <div class="container">
    <a class="navbar-brand" href="#">Snippet</a>

    <div class="navbar-header">
      <button class="navbar-toggle" data-target=".navbar-collapse" data-toggle="collapse" type="button">
        <span class="sr-only"> Toggle navigation </span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
    </div>

    <div class="navbar-collapse collapse">
      <ul class="nav navbar-nav">
        <li ng-class="appCtrl.setActiveClass('home')" ng-click="appCtrl.setScreen('home')">
          <%= link_to dashboard_path, user: current_user do %>
            Home
          <% end %>
        </li>

        <% if current_user %>
            <li ng-class="appCtrl.setActiveClass('user')" ng-click="appCtrl.setScreen('user')">
              <%= link_to dashboard_path, user: current_user do %>
                  <%= current_user.name %>
              <% end %>
            </li>

            <li ng-class="appCtrl.setActiveClass('post')" ng-click="appCtrl.setScreen('post')">
              <a href="#" class="glyphicon glyphicon-comment"></a>
            </li>

            <li ng-class="appCtrl.setActiveClass('profile')" ng-click="appCtrl.setScreen('profile')">
              <%= link_to edit_user_registration_path, user: current_user do %>
                <span class="glyphicon glyphicon-user"></span>
              <% end %>
            </li>
            <li><a href="#" class="glyphicon glyphicon-log-out"></a></li>
        <% end %>
      </ul>
    </div>
  </div>
</nav>